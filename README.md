# notes (orphan branch)

Working notes for SaltyAly's CCC contributions. This branch shares no history
with `main` and never merges into it — catalog changes go through feature
branches and PRs as usual.

- `finos-sync-<date>.md` — sync baselines against finos upstream, repo
  conventions, fork/PR state, and CI pre-flight results per sync.
